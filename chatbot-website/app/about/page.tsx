"use client";

import { useEffect, useRef } from "react";

export default function About() {

    return (
        <main>
            <div>
                <p>This is the about  page.</p>
            </div>
        </main>
      );
}